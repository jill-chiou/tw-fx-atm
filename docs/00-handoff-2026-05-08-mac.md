# 接續工作清單（2026-05-09 → Mac）

> 從這裡接著做，不需要重新理解前因後果。

---

## 目前狀態

| 項目 | 狀態 |
|------|------|
| GitHub Pages | 已上線，定位功能正常（手機測試通過）|
| 資料驗證修復 | **已完成** |
| 抽樣 Google Maps 核對 | **已完成：30/30 通過** |
| 下一步 | index.html 過濾無座標資料 |
| 排版優化 | 待做 |
| 幣別功能 | 待做 |

---

## 資料驗證結果（已完成）

執行了 `scripts/validate_and_fix_geocoding.py`，修復內容：

| 問題 | 筆數 | 處理 |
|------|------|------|
| 多重座標（含 `;`）| 48 | 取第一個值，**已修復** |
| 無座標 | 10 | 保留原樣，前端過濾掉不顯示 |
| 座標超出台灣範圍 | 0 | 無問題（金門 118° 在範圍內）|

**`atm_geocoded.json` 已更新**，多重座標 bug 已清除。

### 10 筆無座標的原因（不需要再追）
- 地址含亂碼（PDF 解析壞掉）：`?林路`、`南?32號`
- 企業 ATM 標注「不對外」，本來就沒有公開地址
- 處理方式：前端直接跳過，不顯示在地圖上

---

## 抽樣驗證結果

執行抽樣驗證流程：

- `data/processed/sample_validation.csv`：30 筆抽樣 Google Maps 連結
- `scripts/review_sample_maps.py`：半自動開啟地圖與紀錄人工判定
- `data/processed/sample_validation_review.csv`：人工核對結果
- `docs/05-geocoding-validation.md`：驗證方法、判定標準、統計說明

判定標準：

> 分行或 ATM 裝設地點在座標點附近，即視為通過。

結果：

| 項目 | 數量 |
|------|------|
| 抽樣筆數 | 30 |
| OK | 30 |
| NG | 0 |
| Skip | 0 |
| 通過率 | 100% |

統計解讀：

- 30 筆抽樣適合作為 MVP 階段的資料品質 sanity check。
- 30 筆全數通過可以支持「沒有發現明顯系統性錯誤」。
- 不能過度宣稱全部 1,900 多筆都 100% 正確。
- 若未來要更嚴謹，可提高抽樣數到 60 或 100 筆以上。

---

## 下一步：修 index.html 過濾無座標的點

目前 `index.html` 遇到 `lat: null` 可能出錯，要加過濾：
在載入 ATM 資料的地方加：
```js
const atms = data.filter(r => r.lat && r.lng);
```

---

## 接下來的順序

1. **資料穩定性** ← 目前在這裡
   - [ ] index.html 加 null 過濾

2. **提交資料驗證成果**
   - [x] 修復 48 筆多重座標
   - [x] 手動核對抽樣 30 筆
   - [x] 新增 `docs/05-geocoding-validation.md`
   - [x] 新增 `scripts/review_sample_maps.py`
   - [x] 產生 `data/processed/sample_validation_review.csv`
   - [ ] commit push

3. **UI 排版優化**
   - RWD 手機版
   - 篩選器整理（銀行太多擠一排）
   - popup 資訊排版

4. **幣別功能**
   - 調查各家資料格式
   - 爬取解析
   - 前端加篩選 UI

5. **整體優化**

---

## 檔案位置快速查詢

| 檔案 | 說明 |
|------|------|
| `index.html` | 地圖主頁 |
| `data/processed/atm_geocoded.json` | 座標資料（已修復）|
| `data/processed/sample_validation.csv` | 30 筆抽樣 Google Maps 連結 |
| `data/processed/sample_validation_review.csv` | 30 筆人工核對結果（全數 OK）|
| `scripts/validate_and_fix_geocoding.py` | 資料驗證修復腳本 |
| `scripts/review_sample_maps.py` | 半自動 Google Maps 人工核對腳本 |
| `docs/05-geocoding-validation.md` | 座標抽樣驗證方法與結果 |
| `docs/todo.md` | 完整待辦清單 |
