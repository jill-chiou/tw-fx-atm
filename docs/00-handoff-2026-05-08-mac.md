# 接續工作清單（2026-05-08 → Mac）

> 從這裡接著做，不需要重新理解前因後果。

---

## 目前狀態

| 項目 | 狀態 |
|------|------|
| GitHub Pages | 已上線，定位功能正常（手機測試通過）|
| 資料驗證修復 | **已完成，尚未 commit** |
| 排版優化 | 待做 |
| 幣別功能 | 待做 |

---

## 資料驗證結果（已修復，待 commit）

執行了 `scripts/validate_and_fix_geocoding.py`，修復內容：

| 問題 | 筆數 | 處理 |
|------|------|------|
| 多重座標（含 `;`）| 48 | 取第一個值，**已修復** |
| 無座標 | 10 | 保留原樣，前端過濾掉不顯示 |
| 座標超出台灣範圍 | 0 | 無問題（金門 118° 在範圍內）|

**`atm_geocoded.json` 已更新**，多重座標 bug 已清除。

### 10 筆無座標的原因（不需要再追）
- 地址含亂碼（PDF 解析壞掉）：`?林路`、`南?32號`
- 企業 ATM 標注「不對外」，本來就沒有公開地址
- 處理方式：前端直接跳過，不顯示在地圖上

---

## 第一件事：確認抽樣核對 + commit

### 1. 手動抽樣核對（30 筆）

打開 `data/processed/sample_validation.csv`，點 Google Maps 連結，確認座標有落在正確銀行地址附近。

### 2. 核對無問題後 commit

```bash
git add data/processed/atm_geocoded.json scripts/validate_and_fix_geocoding.py
git commit -m "fix: resolve multi-coordinate records and validate geocoding data"
git push
```

### 3. 修 index.html：過濾無座標的點

目前 `index.html` 遇到 `lat: null` 可能出錯，要加過濾：
在載入 ATM 資料的地方加：
```js
const atms = data.filter(r => r.lat && r.lng);
```

---

## 接下來的順序

1. **資料驗證** ← 目前在這裡
   - [x] 修復 48 筆多重座標
   - [ ] 手動核對抽樣 30 筆
   - [ ] index.html 加 null 過濾
   - [ ] commit push

2. **UI 排版優化**
   - RWD 手機版
   - 篩選器整理（銀行太多擠一排）
   - popup 資訊排版

3. **幣別功能**
   - 調查各家資料格式
   - 爬取解析
   - 前端加篩選 UI

4. **整體優化**

---

## 檔案位置快速查詢

| 檔案 | 說明 |
|------|------|
| `index.html` | 地圖主頁 |
| `data/processed/atm_geocoded.json` | 座標資料（已修復）|
| `data/processed/sample_validation.csv` | 30 筆抽樣 Google Maps 連結 |
| `scripts/validate_and_fix_geocoding.py` | 資料驗證修復腳本 |
| `docs/todo.md` | 完整待辦清單 |
